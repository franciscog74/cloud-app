services:
  backend:
    build: ./backend_api
    container_name: backend
    ports:
      - "3000:3000"
    env_file:
      - .env

  frontend:
    build: ./frontend_flutter
    container_name: frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    env_file:
      - .env
