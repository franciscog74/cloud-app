version: '3.8'

services:
  backend:
    build: ./backend_api
    ports:
      - "3000:3000"
    env_file:
      - .env

  frontend:
    build: ./frontend_flutter
    ports:
      - "8080:80"
    depends_on:
      - backend
    env_file:
      - .env